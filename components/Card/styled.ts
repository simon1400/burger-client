import styled from "@emotion/styled";
import Link from "next/link";

export const CardS = styled(Link)(({theme}) => `
  margin-bottom: 50px;
  text-align: center;
  text-decoration: none;
  h2{
    margin-bottom: 10px;
  }
`)