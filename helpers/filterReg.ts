import { filterEvents } from "./filterEvents"
import { sortDate } from "./sortDate"

export const filterReg = (arr: any) => {
  // const {future} = 
  // console.log(future)
  // future.map((item: any, idx: number) => {
  //   if()
  // })
  // return future
}